
%dynamic (=>)/2.
%op(100,xfx,'=>').
%room(kitchen).
%is_in(apple, room(kitchen)).
%dynamic (is_in)/2.

%op(15,xfx,=>).
%valor(forma, [ color=>azul , forma=>esferica , peso=>ligero , material=>plastico] , X).
dynamic (=>)/2.
=>(color,azul).
=>(forma,esferica).
=>(peso,ligero).
=>(material,plastico).

	
	
%:-=>(X,Y,Z),write(X),tab(2),write(Y),nl.
%=>(X,Y,Z):-
	%Z = Y, write(Y),nl.
%obtenValor(X,Y,Z):-
%	X=>Y,write(X),tab(2),write(Y),tab(2),write(Z),tab(2),nl.
	
valor(Attr,[H|T],X):-
	=>(Attr,X) = H,!;valor(Attr,T,X).	